server:
  port: 8090
spring:
  application:
    name: ${MMN_BUSINESS_ACCOUNT_NAME}
  profiles:
    active: native
 
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:application;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa
  jpa:
    show-sql: true
    database: H2
    properties:
      hibernate:
        show_sql: true
        format_sql: true      
  h2:
    console:
      enabled: true
      path: /h2-console


#  cloud:
#    config:
#      uri: "http://${MMN_CORE_CONFIG_HOST}:${MMN_CORE_CONFIG_PORT}"
#      fail-fast: true
#      username: ${MMN_CORE_CONFIG_USER}
#      password: ${MMN_CORE_CONFIG_PASSWORD}

  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: crsgyn@gmail.com
    password: xvs950yamaha
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    test-connection: false
  
  