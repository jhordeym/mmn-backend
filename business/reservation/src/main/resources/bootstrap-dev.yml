server:
  port: 8190

reservation.sor_url: 'https://api.saveonresorts.com'

spring:
  application:
    name: ${MMN_BUSINESS_RESERVATION_NAME:business-reservation-service}

  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

